apply plugin: 'groovy'
apply plugin: 'maven'

description 'Gradle plugin for the Weblogic wldeploy ant task'
group 'com.lv.plugins'
version = '1.0.1'

repositories {
        mavenCentral()
        jcenter()
}

dependencies {
    compile localGroovy()
    compile gradleApi()

}